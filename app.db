CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    pseudo VARCHAR(50) NOT NULL UNIQUE
);

CREATE TABLE genres (
    name VARCHAR(50) PRIMARY KEY
);

CREATE TABLE animes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    synopsis TEXT,
    status VARCHAR(20) DEFAULT 'en cours',
    cover_url VARCHAR(500)
);

CREATE TABLE anime_genres (
    anime_id INT,
    genre_name VARCHAR(50),
    PRIMARY KEY (anime_id, genre_name),
    FOREIGN KEY (anime_id) REFERENCES animes(id) ON DELETE CASCADE,
    FOREIGN KEY (genre_name) REFERENCES genres(name) ON DELETE CASCADE
);

CREATE TABLE user_animes (
    user_id INT,
    anime_id INT,
    statut VARCHAR(30) DEFAULT 'À voir',
    note INT CHECK (note >= 1 AND note <= 10),
    PRIMARY KEY (user_id, anime_id),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (anime_id) REFERENCES animes(id) ON DELETE CASCADE
);

-- --- DONNÉES DE TEST ---

INSERT INTO users (pseudo) VALUES 
('Cristaline_Fraise'), 
('Xenojhiiva'),
('JeSus');

INSERT INTO genres (name) VALUES 
('Shonen'), 
('Seinen'), 
('Isekai'),
('Romance');

INSERT INTO animes (name, synopsis, status, cover_url) VALUES 
('Eighty-Six', 'L''histoire dans un monde en guerre Vladilena Milize est attribuee a un escadron', 'termine', 'https://cdn.myanimelist.net/images/anime/1324/110843.jpg'),
('Wandering Witch: The Journey of Elaina', 'L''histoire du voyage d''une sorciere au nom d''Elaina', 'termine', 'https://cdn.myanimelist.net/images/anime/1802/108501.jpg'),
('One Piece', 'Une histoire de pirates qui s''etire en longueur...', 'en cours', 'https://cdn.myanimelist.net/images/anime/1081/137690.jpg');

INSERT INTO anime_genres (anime_id, genre_name) VALUES 
(1, 'Seinen'),
(3, 'Shonen');

